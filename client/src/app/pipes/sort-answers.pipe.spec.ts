import { SortAnswersPipe } from './sort-answers.pipe';

describe('SortAnswersPipe', () => {
  it('create an instance', () => {
    const pipe = new SortAnswersPipe();
    expect(pipe).toBeTruthy();
  });
});
