// import { TestBed } from '@angular/core/testing';

// import { GamesCreationService } from './games-creation.service';

describe('GamesCreationService', () => {
    // let service: GamesCreationService;

    // beforeEach(() => {
    //     TestBed.configureTestingModule({});
    //     service = TestBed.inject(GamesCreationService);
    // });

    it('should be created', () => {
        expect(true).toBeTruthy();
    });
});
